#### 마운트(mount)
장치 장착/인식(하드웨어,드라이버) 과 파일시스템 마운트(논리 연결) 를 분리해 이해하면 정확하다.
마운트는 “스토리지를 경로로 쓸 수 있게 트리에 붙이는 VFS 조작”
정리: 드라이버 인식(attach) → 파티션/LVM → mkfs → mount

`mount /dev/nvme0n1p1 /data` 처럼 파일시스템 인스턴스 <-> 디렉터리(마운트포인트) 를 연결.
이제 `/data/…` 경로로 파일 IO가 가능. 이것이 마운트 그 자체.

```
클라우드에서의 용어 매핑(EBS 예)
Attach: EBS 볼륨을 인스턴스에 붙임 → OS에 /dev/nvme1n1 등장.
mkfs: mkfs.xfs /dev/nvme1n1
mount: mkdir -p /var/lib/db && mount /dev/nvme1n1 /var/lib/db
영구화(fstab): /etc/fstab에 항목 추가해 부팅 시 자동 마운트.
Detach/Terminate 전: umount 로 분리 후 attach 해제.
```

정적 스토리지가 쓰이는 예 클라이언트 파일, 리소스 전달
    오브젝트 스토리지는 PUT/GET 중심, 
    지연이 상대적으로 높고 파일시스템 잠금/저널링이 없음.
    
동적 스토리지가 쓰이는 예
    서버가 실시간으로 읽고 쓰는 가변 데이터. 
    예) 매치 결과, 인벤토리 변경, 길드 상태, 월드 청크 저장, 서버 티크 로그, 크래시 덤프, 설정 롤아웃.

    RDBMS/NoSQL: PostgreSQL/MySQL, Cassandra/Scylla, MongoDB. WAL, 인덱스, 페이지 캐시가 랜덤 I/O를 유발.
    키-값/내장 스토어: RocksDB 기반 서비스, LevelDB, FoundationDB. 작은 레코드의 빈번한 업데이트.
    
레디스 : 레디스는 네트워크 넘어서 접근 가능한 “원격 RAM”
    공유 메모리와 차이는 프로세스 간 포인터 공유, 초저지연이지만 동일 호스트 한정, 동기화(락) 직접 구현 필요.
    레디스 네트워크越, 다언어 클라이언트, 원자 연산 제공, 운영이 쉬움. 대신 네트워크 지연,서버 용량 관리가 필요.
메시지 브로커/로그: Kafka/Pulsar 로그 세그먼트, Raft 스냅샷(Etcd/ZooKeeper). 순차 쓰기 위주지만 재분할,압축 시 랜덤 I/O 발생.
실시간 상태 스냅샷: 월드 세이브, 매치 리플레이 인덱스, 체크포인트.
컨테이너 오케스트레이션의 PV: Kubernetes StatefulSet의 PVC. 재시작 후에도 상태 유지.

SSD,IO가 필요한 이유
    지연 예산: 틱 루프에서 스토리지 p99 지연이 수 ms만 튀어도 전체 프레임이 미끄러질 수 있음. NVMe SSD는 로컬일 때 수십–수백 µs,
    네트워크 부착형은 일반적으로 수 ms 수준이지만 프로비저닝 IOPS로 안정화 가능.
    랜덤 IOPS: 인덱스 갱신과 작은 레코드 쓰기 다발. HDD로는 큐 길이가 급증해 지연 분산이 넓어짐.
    일관성: DB는 fsync, 배리어, 저널링을 요구. SSD + 블록 인터페이스가 이를 보장.

핫 데이터(Key) 현상 : 특정 데이터에 집중되는 트래픽 현상
    테이블 자체에 락을 걸게 될텐데 특정 row에 초 집중(Hot Deal 상품) 하나때문에 느려지는 현상 발쌩

이를 해결하기 위해
        레디스 캐시(in-memory data stducture)로 부하 줄이기, 인메모리 데이터 저장소,
    Hot Key/ 세션 / 랭킹 카운터를 메모리에서 처리
    Read 부하를 대신 받는다. CPU 가용 자원도 높아진다.

    RDS Proxy (커넥션 관리)

----