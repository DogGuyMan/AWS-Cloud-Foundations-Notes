
## Module 7

---

```

[### 1] | B

한 사용자가 회사의 직원 포털에 **로그인한 뒤**, 포털 안에서 **자신의 급여 정보만 조회할 수 있고 다른 직원 정보는 볼 수 없다**고 할 때,  
이 상황에서 “로그인”과 “자신 정보만 접근”에 각각 해당하는 개념은 무엇인가?

A. 로그인: 권한 부여 / 자신 정보만 접근: 인증  
B. 로그인: 인증 / 자신 정보만 접근: 권한 부여  
C. 로그인: 암호화 / 자신 정보만 접근: 인증  
D. 로그인: 인가 / 자신 정보만 접근: 암호화  

---

[### 2] | C

다음 중 **AWS 고객(사용자)의 책임**에 해당하는 것은 무엇입니까?

A. 데이터 센터의 물리적 출입 통제 = AWS
B. EC2 인스턴스가 설치된 서버 랙의 전원 관리   = AWS
C. 애플리케이션 레벨에서의 데이터 암호화 및 접근 제어 = user  
D. 하드웨어 장애 시 디스크 교체  = AWS

---

[### 3] | D

다음 중 **AWS 계정 루트 사용자와 MFA**에 대한 설명으로 가장 적절한 것은 무엇입니까?

A. 루트 사용자는 삭제할 수 있고, 필요하면 다른 사용자에게 위임할 수 있다.  = 삭제 불능, 위임 불능
B. 루트 사용자는 AWS 지원 플랜 변경 권한이 없으므로 MFA를 설정하지 않아도 된다.   = 루트 지원 플랜 변경 가능
C. 루트 사용자는 계정 당 여러 명 생성할 수 있으며, 각각 별도 MFA를 설정해야 한다.   = 루트는 유일함,
D. 루트 사용자는 하나만 존재하며, 강력한 암호와 MFA를 설정하고 일상 업무에는 사용하지 않는 것이 모범 사례이다.  

---

[### 4] | B

다음 중 IAM이 제공하는 **두 가지 접근 방식 유형**을 올바르게 설명한 것은 무엇입니까?

A. 콘솔 액세스와 네트워크 액세스  
B. AWS Management Console 액세스와 프로그래밍 방식(Programmatic) 액세스  
C. 루트 액세스와 읽기 전용 액세스  
D. 온프레미스 액세스와 클라우드 액세스  

---

[### 5] | C

한 개발자는 특정 S3 버킷의 객체를 읽기 전용으로만 사용해야 합니다. 이때 적용해야 할 **보안 모범 사례**는 무엇입니까?

A. 개발자에게 AdministratorAccess 정책을 부여한다.  
B. 개발자를 루트 사용자로 승격한다.  
C. 필요한 S3 버킷 읽기 권한만 포함된 정책을 부여한다.  
D. 개발자를 모든 IAM 그룹에 추가한다.  

---

[### 6] | B

다음 중 IAM 사용자와 IAM 그룹의 차이를 가장 잘 설명한 것은 무엇입니까?

A. IAM 사용자는 여러 명 만들 수 없고, 그룹만 여러 개 만들 수 있다.  
B. IAM 그룹은 IAM 사용자의 집합으로, 한 번에 여러 사용자에게 동일한 권한을 부여하는 데 사용된다.  
C. IAM 그룹은 EC2 인스턴스를 관리하는 데만 사용된다.  
D. IAM 사용자는 임시 자격 증명만 제공하고, 그룹은 영구 자격 증명을 제공한다.  

---

[### 7] | A

다음 중 **IAM 역할(Role)** 의 사용 사례로 가장 적절한 것은 무엇입니까?

A. 한 사람이 두 가지 직무(예: 오전에는 개발자, 오후에는 운영자)를 수행할 때, 필요에 따라 서로 다른 권한 세트를 임시로 수임하여 사용하는 경우  
B. 모든 사용자가 항상 동일한 권한을 갖도록 할 때  
C. 비밀번호 정책을 설정할 때  
D. AWS 계정의 루트 사용자 권한을 영구적으로 다른 사람에게 넘길 때  

---

[### 8] | A

다음 설명 중 **IAM Identity Center와 AWS Secrets Manager의 차이**를 가장 잘 나타낸 것은 무엇입니까?

A. IAM Identity Center는 SSO를 제공하고, Secrets Manager는 애플리케이션/서비스 자격 증명(비밀번호, API 키 등)을 중앙에서 암호화하여 관리한다.  
B. 둘 다 데이터베이스 비밀번호를 저장하는 데만 사용된다.  
C. Secrets Manager는 계정 간 SSO를 제공하고, IAM Identity Center는 API 키를 자동 회전한다.  
D. 둘 다 EC2 인스턴스에 패치를 적용하는 서비스이다.  

---

[### 9] | AB

다음 중 **네트워크 및 애플리케이션 계층에서 DDoS 공격으로부터 보호하는 데 도움을 주는 AWS 서비스**만을 고른 것은 어느 것입니까? (정답 2개)

A. AWS Shield  
B. AWS WAF  
C. Amazon Inspector  
D. AWS Artifact  

---

[### 10] | B

다음 중 **AWS Key Management Service(AWS KMS)** 의 역할을 가장 잘 설명한 것은 무엇입니까?

A. 네트워크 방화벽 규칙을 자동으로 구성해 주는 서비스이다.  
B. 중앙에서 암호화 키를 생성·관리하고, EBS, S3, DynamoDB 등의 서비스에서 데이터를 암호화/해독할 수 있게 한다.  
C. 데이터베이스 쿼리를 최적화해 주는 서비스이다.  
D. EC2 인스턴스의 CPU 사용량을 모니터링하는 서비스이다.  

---

[### 11] | B

다음 설명 중 **저장 중 데이터 암호화(Encryption at rest)와 전송 중 데이터 암호화(Encryption in transit)** 를 올바르게 구분한 것은 무엇입니까?

A. 저장 중 암호화는 SSL/TLS 인증서를 사용하고, 전송 중 암호화는 KMS 키를 사용한다.  
B. 저장 중 암호화는 디스크나 스냅샷에 저장된 데이터를 암호화하는 것이고, 전송 중 암호화는 네트워크를 통해 이동하는 데이터를 SSL/TLS로 암호화하는 것이다.  
C. 둘 다 같은 의미로, 구분하지 않는다.  
D. 저장 중 암호화는 GuardDuty가 담당하고, 전송 중 암호화는 Inspector가 담당한다.  

---

[### 12] | ABC

AWS 계정이 침해되었다고 강하게 의심되는 상황입니다.  
다음 중 **취해야 할 조치**에 해당하는 것만을 모두 고른 것은 어느 것입니까? (정답 3개 이상)

A. AWS Support Center를 통해 온 보안 관련 알림에 응답한다.  
B. 내가 생성하지 않은 리소스를 삭제한다.  
C. 모든 API 액세스 키를 삭제하거나 회전(rotate)한다.  
D. 모든 IAM 사용자 비밀번호를 변경한다.  

---

[### 13] | X

다음 중 **Amazon Inspector와 Amazon GuardDuty의 역할**을 올바르게 비교한 것은 무엇입니까?

A. Inspector는 애플리케이션/호스트 취약점 평에 중점, GuardDuty는 로그·네트워크 활동 기반 이상 징후 및 악의적 활동 탐지에 중점.  
B. 둘 다 규정 준수 보고서를 제공하는 서비스이다.  
C. GuardDuty는 패치를 자동 설치하고, Inspector는 DDoS를 방어한다.  
D. 둘 다 IAM 사용자 비밀번호 정책을 설정하는 데 사용된다.  

---

[### 14] | C

다음 설명과 가장 잘 매칭되는 AWS 서비스를 고르시오.

> “웹 애플리케이션으로 들어오는 HTTP/HTTPS 요청 중, 특정 IP 또는 특정 패턴을 가진 요청을 차단하고 싶다. 이 규칙은 웹 ACL(Web ACL)에 정의한다.”

A. 보안 그룹(Security Group)  
B. AWS Shield  
C. AWS WAF  
D. AWS Certificate Manager (ACM)  

---
```

```
1. B
2. C
3. D
4. B
5. C
6. B
7. A
8. A
9. AB
10. B
11. B
12. ABC
13. 모르겠음
14. C
```

---

> ### 📄 1. 인증 VS 권한 부여

#### 1). 인증 (사용자가 누구인지 확인)

* 인증은 사용자 **이름 및 암호 조합으로 자격 증명**
* 예시
  * 직원이 직원 포털에 **로그인**합니다.

---

#### 2). 권한 부여 (액세스할 수 있는 항목 결정)

* **자격자 라도** 시스템이나 애플리케이션에서 **수행할 수 있는 작업 관리**
* 예시
    * **직원 포털 내에서**만 **자신의 직원 레코드에 액세스**
    * 고객의 권한(고객이 수행할 수 있는 작업)을 확인
      **10,000 USD를 이체할 권리**

---

> ### 📄 2. 고객과 AWS의 보안 관련 책임

#### 1). 고객: 클라우드 내 보안 (Security in the Cloud)

* 자신의 모든 콘텐츠(환자 데이터)를 직접 유지 관리
  데이터, 시스템, 애플리케이션의 보안 관리
* IAM 관리

---

#### 2). AWS: 클라우드 자체 보안 (Security of the Cloud)
* 클라우드 자체의 보안
  * 하드웨어 유지 및 물리적 보안
  * EC2 인스턴스 인프라를 유지 관리하고 패치를 적용할 책임
  * 결함 있는 구성 요소 교체

---

> ### 📄 3. MFA

#### 1). AWS 계정 루트 사용자 (커피숍의 소유주) 

* 모든 리소스에 액세스하고 리소스를 제어
* AWS 계정은 루트 사용자를 1명만 보유 (삭제 불가, 위임 불가)
* AWS 지원 플랜 변경도 루트 사용자의 책임이다.
* 초기 로그인 후 AWS 계정 루트 사용자의 액세스 키를 삭제하는것이 모범 사례다

---

#### 2). 다중 인증(MFA) : 루트 사용자 계정에 강력한 암호를 연결

* AWS Management Console, CLI, API 접근 시에도 적용될 수 있음.
* 액세스 권한을 얻기 위한 2가지 이상의 확인 방법을 제공합니다.
   * 암호, 사용자 이름, 사용자만 보유하고 있는 요소(예: 물리적 토큰)를 요구
   * 일회성 코드를 생성하는 스마트폰 앱
* 신용 카드를 등록은 필수가 아님

---

> ### 📄 4. AWS Identity and Access Management(AWS IAM) 

#### 비밀번호 정책, 사용자/그룹/역할 관리, 자격 증명 관리는 IAM

##### ① "누가 무엇을 할 수 있는가"라는 사용자 및 리소스 권한 구조를 설계하는 데 사용

* IAM 사용자, IAM 그룹,  IAM 역할,  IAM Policy 정의 & 관리
* 각 사용자에 대해 개별 IAM 사용자를 생성
* 사용자마다 고유한 보안 자격 증명 세트를 갖도록 하는 것
  *승인된(Authorized) 액세스는 아니다!*

##### ② 2가지의 IAM 엑세스 제어

1. **AWS 관리 콘솔 액세스**(AWS Management Console access)
2. **프로그래밍 방식 액세스**(Programmatic access)

##### ③ 모범 사례

1. 사용하지 않거나 불필요한 사용자 및 자격 증명을 그대로 두면 안된다.
2. 사용자에게 기본 관리 권한을 부여하지 말고 반드시 **세부적인 액세스 권한을 정의(Define Fine Grain)합니다.**
3. AWS 리소스에 대한 액세스를 관리합니다.
4. AWS Management Console access에 접근하기 위해서 MFA를 활성화 하자.

---

#### 1). IAM 사용자

* **개별 자격 증명 계정 생성**
  * KMS
  * 키를 관리할 수 있는 IAM 사용자 및 역할을 지정
* **처음 생성된 IAM 사용자**에게는 **아무런 권한이 없습니다** 아무것도 못 하죠
* **최소 권한의 원칙** : 권한을 부여할 때는 필요한 경우에만 액세스 권한을 제공

---

#### 2). IAM 그룹

* IAM 사용자 모음
* **한 번에 여러 사용자에게 상속될 액세스 권한**을 할당

---

#### 3). IAM 역할

* **임시로 권한에 액세스**하기 위해 수임할 수 있는 자격 증명
* 이전 역할에 지정된 모든 권한을 포기하고, 새 역할에 지정된 권한을 수임
  오전에는 바리스타로 근무하고, 오후에는 계산원

---

#### 4). IAM 정책

* **JSON 문서**
  * 서비스 및 리소스 액세스 권한을 허용하거나 거부 설정
* 이러한 정책을 IAM 사용자, 그룹, 역할에 주입
특정 서비스, 리소스 대한 액세스 권한을 정의
* IAM 사용자에 S3 버킷 접근 정책(Policy)이 연결되어 있지 않다. -> 권한 부여

---

> ### 📄 5. AWS Manager Service

#### 1). AWS IAM Identity Center
  
##### ① 통합 인증 SSO(Single Sign-On)
*  한 번의 인증 과정으로 여러 컴퓨터 상의 자원을 이용 가능하게 하는 인증 기능
* 기존 organizations들에게 하여금 AWS 리소스 사용에 SSO를 적용하도록 설계됨

##### ② AWS에 대한 엑세스 중앙 관리

* 데이터베이스 자격 증명 및 API 키를 저장하거나 관리하기 위한 용도로 설계되지 않음.

---

#### 2). AWS Secrets Manager

##### ① 자격 증명 관리 및 수명 주기

* 전체 수명 주기 동안 서비스, 리소스 자격 증명 
* API 키 및, 기타 비밀 암호를 같은 민감한 정보를 관리
  * 자동 교체 
  * 액세스 제어

##### ② 중앙 위치에 저장

---

#### 3). AWS Systems Manager

* 대규모 AWS 리소스 노드 또는 연결 지점을 보안 패치 적용을 자동화

---

> ### 📄 6. 네트워크 및 애플리케이션 보호

#### 1). Dos VS DDos

##### ① DoS 
* 혼자 웹 애플리케이션에 과도한 네트워크 트래픽을 몰아서 전송
* 과부하가 발생, 여러사이트 표적은 아님

##### ② DDoS
* 좀비 피시 여러 컴퓨터와 장치를 사용하여 공격을 시작
* 과부하가 발생, 여러사이트 표적은 아님

---

#### 2). 내장형 구성요소

1. **보안 그룹** : 인증된 사용자의 트래픽만 시스템에 들어오도록 허용
2. **Elastic Load Balancing(ELB)**
   * ELB는 수신 트래픽을 분산
   * 대규모 공격을 흡수 과부하가 발생하는 것을 방지
3. **AWS 리전** : 리전은 용량이 매우 크므로 과부하를 일으키기가 상당히 어렵습니다. 

---

#### 3). 보호 서비스

##### ① AWS Shield

* DDoS 공격 보호 & 실시간으로 탐지하고 완화
* 무료로 보호하는 관리형 DDoS 보호 서비스
* (CloudFront, Amazon Route 53, ELB 같은 다른 서비스와도 통합)

##### ② AWS WAF

* 웹 ACL에서 정의한 차단된 IP 주소로부터 네트워크 요청을 필터링 보호

##### ③ AWS Certificate Manager (ACM)

* SSL/TLS 인증서 
* 전송 중 데이터 암호화를 제공

---

> ### 📄 7. 데이터 보호 암호화

#### AWS Key Management Service(AWS KMS)

* 중앙 집중식 암호화 키를 생성하고 관리
  * 생성한 암호화 키를 사용하여 데이터를 암호화하고 해독
    1. 암호 : 데이터 잠금
    2. 암호 해독 : 잠금 해제에 사용
  * 광범위한 서비스 및 애플리케이션에서 키 사용을 제어

* KMS를 관리하는 IAM 계정이 다음 책임이 있다.
    * 키 정책/권한/회전/사용 결정(암호화 적용 선택)

* 기능은 스토리지의 민감한 데이터를 암호화하는 데 사용됩니다.
    1. EBS : 스냅샷 저장시에 암호화
    2. S3 : 기본적으로 모든 새 S3 버킷에는 암호화가 구성
      저장 중인 데이터는 암호화 되긴 하는데
      전송 중인 데이터를 암호화하지는 않음
    3. DynamoDB : KMS와 연계하여 보호

---

> ### 📄 8. 탐지

#### AWS 계정이 침해되었다고 의심될 때, 해야 할 조치
1. **AWS Support Center를 통해 온 알림에 응답**
2. **내가 만들지 않은 리소스 삭제**
3. **모든 API 액세스 키 삭제/로테이션**
4. ⭐️**모든 IAM 사용자 비밀번호 변경**⭐️

---

#### 1). Amazon Inspector

##### EC2 등 호스트/애플리케이션의 취약점 스캔

---

#### 2). Amazon GuardDuty

##### CloudTrail, VPC Flow Logs, DNS 로그 등을 분석해서 “이상 징후 / 악의적인 활동 탐지”
