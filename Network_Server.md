### 서버란..

https://chatgpt.com/g/g-aCkxQDRNh-network-engineer-guru/c/69140565-b2c4-8321-b2a4-1fade0268410

* 애플리케이션 서버와 DB서버가 한 컴퓨터에서 함께 돌 수도 있고, 
N개의 ‘서버’가 반드시 N대의 컴퓨터일 필요는 없다

* “서버”는 보통 소프트웨어 서비스(프로세스/컨테이너/VM) 를 가리킨다. 
하나의 물리/가상 머신에 여러 서버(서비스) 를 띄우는 건 흔하다.

* 다만 물리 한계(코어/메모리/SSD IOPS/네트워크 대역) 를 넘을 수 없고, 
단일 장애점(SPOF) 은 그대로다.

### 데이터 베이스 서버는 왜 EC2 + EBS 스펙일까의 근거

```
데이터를 저장하는 스토리지라 하면 S3도 있을것 이다.
어쩄든 여기도 데이터를 저장하니 EC2와 S3의 연결을 사용하면 안되냐?
라고 생각했는데 절대 그러면 안될것 같다

왜냐하면 S3는 오브젝트 단위 스토리지이고, 정적 파일이므로
게시 하는거면 한꺼번에 올리거나, 삭제하면 한꺼번에 삭제될 것이라 생각한다.
Readonly 데이터라면 즉, 런타임중 변경될 여지가 절대로 없을 데이터베이스 서비스라면
EC2 + S3도 충분이 못할것은 없어 보인다. 그리고 오히려 좋다. 수용량이 이론상 무한대일 터.

하지만 데이터베이스는 저렇게 작동될 수 없다.
한 스키마 내부의 테이블 내부의 일부 레코드를 수정하거나, 
같은 테이블의 레코드를 추가하거나 삭제하든지, 
다른 테이블을 옮겨가며 데이터를 읽어야 할 수 있다.
그것도 실시간으로 매우 많은 요청과 처리를 아주 빠르게 해야 하므로
이는 동적 데이터에 해당되는 것 이고, 
EBS 여야만이 가능한 것인 EBS는 대표적인 동적 데이터 스토리지 이고,
데이터 내의 일부 요소를 변경할 수 있다는 점때문이다.
```

#### 운영(OLTP) DB의 핵심 I/O 요구가 ‘블록 장치’ 를 필요로 하고, AWS에서 그 역할을 대표적으로 EBS가 제공한다는 점이 본질이다.

* 대신 백업,아카이브,분석에 최적에는 쓸 수 있다.
    ```
    S3가 부적합 한 근거
    1. S3: 객체 단위 PUT/GET/DELETE(HTTP). 
        한 번의 작은 쓰기에도 수 ms~수십 ms 수준의 오버헤드
    2. 부분 업데이트 없음, 
    3. 파일시스템,락,fsync() 없음.
    4. S3는 오브젝트 스토리지라서 DB라면 필요로 하는 바이트 단위,페이지 단위의 임의 쓰기(in-place random write)를 제공하지 않는다.
    ```

* `fsync()`라는 말은 **동기 쓰기** 라는말로 대체해서 생각해도 된다.
즉 **동기 쓰기는 DB 트랜젝션과 동일하다**
    ```
    블럭 스토리지는 두가지 점에서 DB 서버에 적합하다.
    1. 트랜잭션 커밋 시 WAL/Redo를 동기 쓰기(fsync) 지원
    2. 데이터/인덱스 페이지를 랜덤 읽기/쓰기
    ```