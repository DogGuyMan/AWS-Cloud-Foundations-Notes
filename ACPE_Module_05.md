
---

## Module 5

---

### 문제


```
D. 자동 리전 간 복제 & 실시간 모니터링 (자료 상 X)
S3 자체 기능으로 보지 않고, CRR 설정 및 CloudWatch 등 별도 서비스 사용하는 것으로 분리해서 설명하는 경우가 많다.
다만 실제 AWS에선 CRR/Replication, CloudWatch Metrics로 가능하므로, 시험에서라면 C는 분명 오답이고 A,B가 안전한 정답 세트.

---


17번
정답: A, C

A. NFS 기반, 멀티 인스턴스 동시 읽기/쓰기 (O)
EFS의 핵심.
C. 리전 내 멀티-AZ 자동 복제 (O)
고가용성·내구성 제공.

정답: A, C

A. 온프레/클라우드 서버를 AWS로 지속 복제, 장애 시 빠르게 복구 (O)
EDR의 핵심 목적.
C. 저렴한 스토리지·최소 컴퓨팅으로 RPO/RTO 최소화에 초점 (O)
DR 비용 효율+복구 목표 최소화.

26번
정답: A, B

A. 동종/이기종 DB 간 복제·마이그레이션 (O)
DMS 핵심.
B. 마이그레이션 중에도 소스 DB 계속 사용 가능, 다운타임 최소화 (O)
CDC(Change Data Capture)로 동기화.
C. 애플리케이션 코드를 다른 언어로 변환 (X)
그런 기능 없음.
D. 모든 EC2 상태 모니터링 (X)
CloudWatch/CloudTrail 영역.
```

---

> ### 📄 **Amazon Instance Store**
#### 1. 기본 개념
1. 최고의 I/O 성능 , Low Latency
    * 피크 처리 시간대에 성능 병목 현상해결
    * 단일 인스턴스 워크로드의 지연 시간 최소화에 유리하고, 
2. Amazon EC2 인스턴스에 직접 연결 
3. 종료되면 데이터가 계속 유지되지 않고 임시 저장한다.
4. 이벤트가 아주 많고, 분석 용도에는 적합하지 않음

---

> ### 📄 **Amazon EBS 핵심 정리 (모든 내용 포함, 축약본)**  

#### 일관되고 지연 시간이 짧은 접근에 유용함

#### 1. EBS 기본 개념
1. **인스턴스 종료 후에도 유지되는 영구 스토리지 볼륨을 제공**  
    * 필요에 따라 연결하거나 분리할 수 있습니다.
2. **SSD/HDD 기반 내구성 스토리지** 
    * 금융 워크로드에 필요한 **높은 가용성,내구성** 제공  
3. 빠른 동적 데이터
    * **짧은 지연 시간**이 필요한 **재무/금융 애플리케이션**에 적합  
    * **메모리 캐시는 아님**  
4. Security Transparency 제공
   * 즉 인스턴스에서 암호화되지 않은 것처럼 착각 되지만
   * 생성 시 데이터 암호화 제공

#### 2. 가용성,리전 특성

* **동일 가용 영역(AZ) 내 자동 복제**로 가용성과 내구성 확보  
* EBS 볼륨은 **리전,AZ 종속**
  * 추가 구성 없이는 **리전 간 직접 공유 불가**  
  * 리전 간 사용 시 **지연 시간 증가 가능**

#### 3. 성능,확장

* **IOPS/성능**
  * 볼륨 **유형**(gp3, io2 등)과 **크기**에 따라 다름  
  * 성능은 **무제한이 아니며**, 종종 **볼륨 크기에 비례**

* **확장 방식 : 자동으로 주기 실행되지 않음**
  * 용량을 늘리려면 사용자가  
    1) **볼륨 크기 확장**,  
    2) **파일 시스템 확장**을 명시적으로 수행해야 함  

#### 4. EBS 스냅샷

* EBS 볼륨의 **특정 시점(point-in-time) 상태를 S3에 저장하는 스냅샷**  
    * 스냅샷으로부터 **기존 데이터 기반 새 볼륨을 빠르게 생성** 가능  
* 이를 통해:
  * **프로덕션 환경을 미러링**한 테스트/스테이징 환경 구성  
  * **일관된 테스트용 환경 사본**을 반복적으로 생성하기에 적합  

#### 5. 스냅샷 운영 및 한계

* **자동화 관련**
  * 하지만 **리전 간 데이터 마이그레이션을 자동으로 수행하지는 않음**  
  * 혼자서는 자동화 못함 Data Lifecycle Manager를 써야함
* **무엇을 하지 않는가**
  * 볼륨 데이터를 **특정 비율로 압축**해 주는 기능 아님  
  * **성능 최적화**를 수행하지 않음

---

> ### 📄 AWS Data Lifecycle Manager

#### 1. 스냅샷 자동 관리

* EBS 스냅샷 및 EBS 기반 Amazon Machine Image(AMI)의 생성, 보존, 삭제 작업 자동화
    * 스냅샷을 수동으로 관리할 필요가 없습니다.
    * 성능 모니터링이 핵심이 아님.
    * 데이터 암호화를 처리하지 않습니다.

#### 2. 리소스 수명 주기
* 데이터 백업 및 보존
* 비용 관리를 위해 오래된 스냅샷을 제거해야 할 떄 적합한데

---

> ### 📄 S3
#### 1. S3 기본 개념,용도

1. **높은 신뢰성 객체 스토리지**
   * 기본적으로 모든 새 S3 버킷에는 암호화가 구성
   * "단일 객체 최대 **5TB**"
3. URL을 통한 간편한 파일 공유, HTTP/HTTPS
4. 대표 사용 사례  
  * **사진,동영상 등 사용자 생성 미디어** 저장/배포  
  * 데이터 레이크 : AI 학습에서 거의 모든 양의 정형 또는 비정형 데이터를 저장
  * 로그 이벤트 : CloudTrail & CloudWatch

#### 2. 퍼블릭으로 열기 위한 기본 설정

* S3 버킷의 이름은 **전 세계 모든 AWS 계정에서 고유**해야 합니다.

* 버킷 정책,ACL, IAM : 기본적으로는 public 하지 않다.
* **S3 Block Public Access**
    * 계정/버킷 수준 접근 제어
    * 버킷 정책을 덮어씀으로 퍼블릭 접근을 차단함  
* 태그는 접근 권한과 무관, 누구나 컨텐츠 접근에 있어서 객체 태그는 전혀 필요 없음

#### 3. 가용성,리전 특성

* S3는 **동일 리전 내 멀티-AZ 복제**로 가용성과 내구성 확보  
  * 리전 간 까지 복제하지 않는다
* Amazon S3에서 버킷을 생성하면 특정 AWS 리전과 종속된다.

* **가용성(Availability) 등급**
$$
\text{Standard(99.99)} \geq \text{Reduced Redundancy(99.99)} > \text{Infrequent Access (IA) (99.90)}
$$


#### 4. S3 스토리지 클래스

* **스토리지 클래스의 두가지 지표에 따른 등급**
  1. **Durability(내구성)** : 데이터가 사라지지 않을 확률 (예: 11 nines)
  2. **Availability(가용성)** : 필요할 때 접근 가능한 비율 로 스펙이 명시된다

* **비용 차이** 
  * 스토리지 비용, 검색 수수료, 최소 보관 기간에 따라 달라집니다.

* **수명 주기 : "스토리지 클래스간 전환 자동화", "객체 자동 삭제"**
  * 0~30일: 자주 액세스 → **S3 Standard**  
  * 30일 후: 가끔 액세스 → **S3 Standard-IA(Infraquent-Access)**로 전환  
  * 90일 후: 거의 미사용 → **S3 Glacier**로 전환  
    

* **접근 주기** : 지연 없이 바로 접근 가능
  * 접근 패턴이 알 수 없거나, 자주 바뀌는 데이터 → **S3 Intelligent-Tiering**  
  * 조회/복구 시간이 길고(분~시간), GB당 비용이 매우 저렴하지만, 조회에 비용이 발생. → **S3 Glacier**

* **운영 방식**
  * 모범적인 운영은 **최소 30일 이후에 마이그레이션**하도록 라이프사이클 정책을 만든다. 
  * 즉시 Glacier로 보내 복원 정책을 직접 관리하는 기능이 아니라,  
    **"언제 어떤 클래스로/삭제할지"를 규칙으로 지정**하는 것  

#### 5. 요금


#### 7. 무엇을 하지 않는가  

* ❌ SQL 쿼리,인증 등 **관계형 DB 기능**  
* ❌ VM 호스팅,코드 실행 등 **컴퓨팅 서비스**  
* ❌ **실시간 스트리밍,메시지 처리**  
* "트랜스코딩 X"
* "자동 압축 X"
* "리전 간 복제(X)"
* "모니터링/알림(X)"


---

> ### 📄 Amazon EFS 핵심 요약

#### 1. **네트워크 파일 시스템(NFS) 기반 공유 파일 스토리지**
* Linux 기반 애플리케이션
* **블록 스토리지(EBS)나 로컬 인스턴스 스토리지가 아님**
  *  EFS는 모든 파일 시스템으로 포맷할 수 있는 블록 스토리지를 제공안함
  *  EFS는 전용 처리를 위한 인스턴스 액세스를 제공하지도 않음
* **단일 리전 내 여러 가용 영역(AZ)에 자동 복제**하여 내구성,가용성 제공  
  (전 세계 여러 리전에 자동 복제하는 글로벌 파일 시스템은 아님)

#### 2. 공유 액세스,협업
* 서로 다른 AZ에 있는 **수천 개 EC2 인스턴스에서 동시 공유 액세스** 제공
  * 여러 인스턴스가 동일한 데이터에 동시에 접근 가능한 파일 스토리지.
* **수동으로 동기화 X**
* **여러 서버/여러 팀이 동일한 파일에 협업** 

#### 3. 자동으로 확장 및 축소
* 데이터가 추가,삭제될 때 **파일 시스템 용량,처리량이 자동으로 확장/축소**

#### 4. 지연 시간 특성
* **EFS는 다수 인스턴스의 공유 액세스,고가용성에 최적화**되어 있다.
    * 로컬 인스턴스 스토리지는 단일 인스턴스 워크로드의 지연 시간 최소화에 유리하고,  

---

> ### 📄 Amazon FSx
* 널리 사용되는 파일 시스템을 기반으로 파일 스토리지를 제공하는 완전관리형 서비스
* 애플리케이션과의 호환성을 유지하면서, 파일 공유를 AWS로 마이그레이션

---

> ### 📄 Amazon FSx for Windows File Server
* 완전관리형 Windows 파일 시스템
* Microsoft Active Directory와 통합되는 솔루션

---

> ### 📄 AWS Storage Gateway

#### 1. AWS Storage Gateway

* 온프레미스(사내 데이터센터) 환경과 AWS 스토리지(S3/EBS/테이프)를 연동해 로컬처럼 쓰게 해줌
  * 하이브리드 스토리지 서비스
  * 자주 사용하는 파일에는 로컬 액세스를 유지하고자 합니다. 
  * 기존 파일 공유 워크플로와 애플리케이션에 대한 변경을 최소화
* 무제한 클라우드 스토리지에 대한 액세스
* **파일 공유가 목적이 아님, 블록 단위 볼륨** 인터페이스

#### 2. File Gateway

* 온프레미스 : S3를 Gateway에 배포하면 
  * **파일 인터페이스(SMB/NFS)** 제공하고 S3가 로컬에 있는 착각을 하게 된다.
  * 따라서 일반 파일 작업하듯사용해도 자동으로 Amazon S3에 업로드한다.
* AWS : **Amazon S3 객체**로 저장
  * 기존 파일 공유 워크플로와
  * 애플리케이션에 대한 변경을 최소화

#### 3. Volume Gateway 

##### 가상 스토리지 볼륨을 생성한다.
* **파일 공유가 아닌** iSCSI 블록 스토리지 볼륨으로 제공
* **변경 자주있음**

##### ① 캐시(Cached) 모드

* 온프레미스 : **자주 쓰는 데이터만 로컬 캐시**
* AWS : 나머지는 가상 스토리지 볼륨에 저장
  
##### ② 저장(Stored) 모드

* 온프레미스 : **모든 데이터를 로컬**
* AWS : EBS 스냅샷으로 **백업/복구용 사본만 백업**

#### 4. Tape Gateway

* 온프레미스 : 수준 액세스를 못함
* AWS : 백업,아카이빙용 가상 테이프는 **Amazon S3/S3 Glacier**에 저장

---

> ### 📄 **AWS Elastic Disaster Recovery**  

#### 1. 지속적인 짧은 간격으로 계속 복제
* 별도의 복제 서비스와 통합하지 않아도 **DR 구성이 가능**  

#### 2. 신속한 복구
* AWS 리전 내, 온프레미스 및 클라우드 기반 
* 가동 중단 시간과 데이터 손실을 최소화하는 안정적인 복구 서비스

#### 3. 성능

* 저렴한 스토리지, 최소한의 컴퓨팅 리소스, 특정 시점 복구 기능

#### 4. 독립성
* 에이전트만 추가로 설치하면 되며,  
* 기존에 쓰던 **백업 소프트웨어를 제거하거나 교체할 필요는 없음**  


> ### 📄 AWS Database Migration Service (AWS DMS)

* 한 데이터베이스에서 다른 데이터베이스로 데이터를 복제,이관할 때 쓰는 관리형 서비스.
  1. 온프레미스 → 클라우드 마이그레이션, 
  2. MySQL → PostgreSQL 등
* 마이그레이션 중에도 소스 DB를 계속 사용 가능(다운타임을 최소화하는 구조 지원).